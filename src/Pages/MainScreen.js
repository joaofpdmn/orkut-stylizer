import React from "react";
import Header from "../common/Header";
import Padding from "../common/Padding";

export default function MainScreen({ name, status }) {
    return (
        <>
            <Padding value={100} />
            <Header />
        </>
    )
}